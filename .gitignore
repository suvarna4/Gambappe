# deps
node_modules/

# builds
dist/
.next/
.turbo/
*.tsbuildinfo

# tests
coverage/
playwright-report/
test-results/
blob-report/

# env & local
.env
.env.local
.env.*.local
*.log

# editor/os
.DS_Store
.idea/
.vscode/

# agent tooling (isolated worktrees for background/parallel agent sessions)
.claude/

# local redis-server dumps (e.g. manual dev testing outside docker-compose)
*.rdb

# WS14-T2 load-test fixtures (ghost cookies + generated slugs — regenerated by
# `apps/web/load-tests/seed.ts` per run, never meant to be committed)
apps/web/load-tests/.fixtures/
.vercel
.ccr-ca.crt
