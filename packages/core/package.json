{
  "name": "@receipts/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "THE CONTRACT HUB (design doc §4.2): constants, error codes, enums, branded ids, zod API schemas, domain types, flags, clock.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@receipts/config": "workspace:*",
    "@types/node": "^22.20.1",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
