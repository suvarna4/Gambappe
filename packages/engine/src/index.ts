/**
 * @receipts/engine — pure functions over plain data (design doc §8). No DB, no clock reads
 * (time is a parameter), all constants from @receipts/core config.
 *
 * WS0 scaffolds the package; the engine itself is WS4 scope (fingerprint §8.1–8.2,
 * Glicko-2 §8.3, matchers §8.4–8.5, scorers §8.8–8.9, narration §13.3).
 */
export {};
