/**
 * @receipts/ui — design-system components (design doc §10.4: TicketCard, Stamp, PriceTag,
 * CrowdBar, CountdownTicker, StreakFlame, Barcode). WS7-T1 scope; scaffolded in WS0 so the
 * §4.1 tree and dependency graph are in place.
 */
export {};
